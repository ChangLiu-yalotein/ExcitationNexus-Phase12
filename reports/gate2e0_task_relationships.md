# Gate 2-E0 train-only task relationships

All correlations use IID train with group weights. The 12-secondary standardized covariance rank is 12; condition number is 8.788e+08; effective task count is 3.607.

The fixed pairwise numeric redundancy gate produced 0 candidate pair(s). Physical/schema support remains mandatory. Separately, the known multivariate descriptor identity `t = D - H_CT` makes `tddft_t_index_angstrom` report-only redundant.

No model, primary residual, validation correlation, or prediction was produced.

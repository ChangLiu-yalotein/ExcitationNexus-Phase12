# Gate 0-D GPU smoke

Status: **GPU_DONE** on physical GPU 0 (`cuda:0`).

Two FP32 batches of size 4 completed forward, masked loss, backward, and AdamW step in 1.643 s.
Peak allocated/reserved memory: 32.7/46.0 MiB.
Finite loss/gradients, nonzero shared/primary gradients, parameter change, and GPU translation/rotation invariance all passed.

No epoch, DDP, checkpoint, model comparison, or selection was run.
